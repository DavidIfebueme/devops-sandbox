global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "sandbox-api"
    static_configs:
      - targets: ["host.docker.internal:8080"]

  - job_name: "nginx"
    static_configs:
      - targets: ["sandbox-nginx:80"]
